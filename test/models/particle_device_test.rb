require 'test_helper'

class ParticleDeviceTest < ActiveSupport::TestCase
  # TODO: Remove file? the particle_device is onty ever accessed via Rhizome, so the validations are all tested there. Can move tests here if need be.
end
