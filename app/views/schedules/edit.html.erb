<% provide(:title, 'Edit Schedule') %>
<h1>Edit Schedule</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_for(@schedule) do |f| %>
        <%= render partial: 'fields', locals: {f: f, object: @schedule} %>
        <%= button_tag 'Save', type: :submit, class: 'btn btn-sm btn-primary' %>
        <%= link_to 'Cancel', schedules_path, class: 'btn btn-sm btn-primary btn-danger' %>
    <% end %>
  </div>
</div>
